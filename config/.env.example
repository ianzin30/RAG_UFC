UFC_API_KEY=
UFC_API_URL=

TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
SCRAPING_API_KEY=
FIRECRAWL_API_KEY=
SCRAPING_API_URL=

# Optional: override the default Google OAuth desktop credentials path.
# Runtime tuning like model, embedding device, batch size, max length, and
# quantization now lives in `config/config.toml`.
GOOGLE_OAUTH_CREDENTIALS_FILE=./config/google-oauth-credentials.json
